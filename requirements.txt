# Core library dependencies -- notice there is no database driver here.
# The core library (src/) has no default repository or database
# dependency; see docs/extending-the-repository.md. Install
# examples/postgres/requirements.txt separately if you're using that
# example.
boto3>=1.34
pytest>=8.0
